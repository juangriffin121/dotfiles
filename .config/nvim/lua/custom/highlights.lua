local M = {}

M.override = {
  -- Override statusline highlights
  -- StatusLine = { bg = "none" },
  -- StatusLineNC = { bg = "none" },
  -- If you use a tabline, override its groups as well
  -- TabLine = { bg = "none" },
  -- TabLineSel = { bg = "none" },
  -- TabLineFill = { bg = "none" },
  -- Optionally, if your colorscheme creates custom groups for the statusline,
  -- override them here:
  -- CatppuccinStatusLine = { bg = "none" },
  -- CatppuccinTabLine = { bg = "none" },
}

M.add = {}

return M
